#import "../module/module.typ": *
#show: module

#link("../item/notice.html")[中文] | #link("../item_en/notice_en.html")[English]

整理和记录的想法, 关于数学, 不保证正确性和可读性和细节完整性, 有无证明的猜测. 目前很多只能算是 *草稿*. 可能随时需要修改错误或需要改进不好的处理或需要删改大量的东西

(左上角放大镜图标, 或 `s` 键打开, `esc` 关闭) 用搜索功能来搜索 label. 例子: 对带下划线的 `123`, 其地址后方是 `#456` (点击下划线的文本就可以在浏览器地址栏看到), 应该在搜索框输入 `[456]` or `[456` (应该用 javascript 自动化这些操作, 但是我不会)

你也可以用 #link("https://github.com/ecbc0/note-math")[typst 文件] 和 #link("https://github.com/Myriad-Dreamin/tinymist")[tinymist] 的 preview 功能, 来看这份文档

Typst 的 HTML 输出还没完善, 所以显示效果可能不太好