#import "/module/module.typ": *
#show: module

$𝕊^3,𝕊^2,𝕊^1$ or 复数 $ℂ$ 版本的 Hopf-bundle 

#tag("Hopf-bundle")
#indent[
  嵌入 $𝕊^2 ↪ ℝ^3 ↪ ℂ^2$, in $ℝ^3 ≃ ℝ × ℂ$ 使用 $w/z ∈ ℂ$ 作为球极投影坐标
  $
    dmat( delim: #none , 
      ℂ^2 , ⟶ , ℂ , ⟶ , ℝ × ℂ ;

      z \, w , ↠ , w/z , ⟶ , (1 - abs(w/z)^2, 2 w/z)/(1 + abs(w/z)^2) ;

      ,,, = , (|z|^2 - |w|^2, 2 w z^*)/(|z|^2 + |w|^2) ;

      , ↠ , z^* /w^* , ⟶ , (- (1 - abs(z/w)^2), 2 (z^*)/(w^*))/(1 + abs(z/w)^2)
    )
  $
  球极投影两个坐标的转换函数 $w/z ⇝ z^* / w^*$ or $ξ ⇝ 1/ξ^*$

  $λ(z,w),λ ∈ GL(1,ℂ)$ 不改变射影结果 e.g. $(λ z)/(λ w) = z/w$
  
  $ℂ^2 ∖ 0$ 是 $𝕊^2 = ℂℙ^1$ 上的 $GL(1,ℂ)$ 丛
  
  用两个球极投影坐标来构造丛坐标
  
  $(z,w) ⇝ (2 w/z, z)$ 和 $(z,w) ⇝ (2 z/w, w)$
  
  以及转换函数 $(2 w/z, z) = (2 (z^*)/(w^*), w^*)$ or $(ξ,λ) ⇝ (1/ξ^*,(1/2 ξ λ)^*)$

  可以先 quotient $ℂ^2 / ℝ_(> 0) ≃ 𝕊^3 = {|z|^2 + |w|^2 = 1}$
  
  此时球极投影的 $ℝ^3 ≃ ℝ × ℂ$ 表示
  
  $ (z,w) ↠ (|z|^2 - |w|^2, 2 w z^*) $ 
  
  $λ(z,w),λ ∈ #U (1)$ 不改变射影结果
  
  $𝕊^3$ 是 $𝕊^2$ 上的 $#U (1)$ 丛
  
  用两个球极投影坐标来构造丛坐标 
  
  $(z,w) ⇝ (2 w/z, z/(|z|))$ 和 $(z,w) ⇝ (2 z/w, w/(|w|))$
  
  以及转换函数 $(2 w/z, z/(|z|)) = (2 (z^*)/(w^*), (w^*)/(|w^*|))$ or $(ξ,λ) ⇝ (1/ξ^*, (ξ/(|ξ|) λ)^*)$
]